{
  "functions": {
    "api/**/*.mjs": {
      "runtime": "@vercel/node"
    }
  },
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
